/**
 * Factory for geolocation
 * commented us example
 */

// loads to init file (require)
define(['./init'], function (services, io) {
    'use strict';

    services.factory('geolocation', [function () {

    }]);
});